# Python dependencies for Google to Apple Photos Metadata Synchronizer

# Core dependencies - большинство библиотек входят в стандартную библиотеку Python 3.6+

# Для хеширования изображений и сравнения
Pillow>=9.0.0  # Для работы с изображениями
imagehash>=4.3.0  # Для перцептивного хеширования изображений

# Внешние зависимости
# exiftool - должен быть установлен в системе
